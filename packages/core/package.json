{
  "name": "@prospectwarm/core",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test:unit": "pnpm run build && node --test tests/*.test.mjs",
    "build": "tsc -p tsconfig.json"
  }
}
