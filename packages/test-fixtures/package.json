{
  "name": "@prospectwarm/test-fixtures",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test:unit": "echo \"No fixture unit tests yet\"",
    "build": "tsc -p tsconfig.json"
  }
}
